name: Publicar Eletromix 10.10.33 (histórico)
on:
  workflow_dispatch:
jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow histórico desativado; use a versão atual."
